{% extends 'base.html.twig' %}

{% block title %}{{ 'Test Js' }}{% endblock %}

{% block body %}
    <div class="row p-3">
        <div class="col-6">
            <a class="btn btn-sm btn-warning" href="{{ path('test_home') }}">{{ 'Test Home' }}</a>
        </div>
        <div class="col-6">
            <a class="btn btn-sm btn-danger" href="{{ path('admin_home') }}">{{ 'Admin' }}</a>
        </div>
    </div>
    <div class="row p-3">
        <div class="col-6">
            <a class="btn btn-sm btn-info" href="{{ path('sandbox') }}">{{ 'Zone JS' }}</a>
        </div>
    </div>
{% endblock %}

